import React from "react";

// Placeholder component to avoid export of non-component symbols from components/ folder.
// The real toast context and hook live in `src/contexts/ToastContext.js`.
export default function ToastContextPlaceholder() {
  return null;
}
